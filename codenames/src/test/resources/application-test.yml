spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/clasified_files}
    username: ${DB_USER:red_panda_us}
    password: ${DB_PASSWORD:red_panda_passw}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    property-naming-strategy: SNAKE_CASE

jwt:
  secret: esta_es_una_clave_secreta_muy_larga_para_pruebas_unitarias_y_de_integracion_2024
  expiration-ms: 3600000

game:
  profanity-blacklist: tonto,mierda,puto
  tema-basico-id: 1
  timer-pool-size: 2
  penalizacion-abandono: 10
  balas-ganador: 20
  balas-derrota: 10
